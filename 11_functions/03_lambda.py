# lambda functions -> are anonymous, one-line functions (no need for def and return keywords)

# lambda function to add two numbers
add = lambda a, b: a+b
print(f"Addition: {add(5,4)}")

# lamda function to sqaure a number
square = lambda x: x*x
print(f"Square: {square(5)}")