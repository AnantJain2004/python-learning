# Tuple - ordered but immutable collection of items(cannot be changed after creation)

my_tuple = (10, 20, 30)
print(my_tuple)

# Tuple with one element
singleton_tuple = (25, ) # comma required
print(singleton_tuple)